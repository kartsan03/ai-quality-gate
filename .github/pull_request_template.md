## Summary

## Changes

## Checklist

- [ ] `pytest` passes
- [ ] New check: tests added, examples in `passing/` and `regressions/`, entry in `docs/checks.md`, row in README table
- [ ] No model calls, no network, no new runtime dependencies
- [ ] Docs updated where behavior changed
